name: Lint & Check Prose
on: [pull_request]

jobs:
  vale:
    name: runner / vale
    runs-on: ubuntu-latest
    steps:
      - name: "Load project locally"
      - uses: actions/checkout@v3

      - name: "Obtain list of files changed in this branch"
      - run |
           echo "Current branch: ${{ GITHUB_HEAD_REF }}"
           echo "Merge to branch: ${{ GITHUB_BASE_REF }}"

      - name: "Run Vale"
#      - uses: errata-ai/vale-action@reviewdog
#        with:
#          reporter: github-pr-check
#          fail_on_error: true
