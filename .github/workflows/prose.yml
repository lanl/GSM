name: Lint & Check Prose
on: [pull_request]

jobs:
  vale:
    name: runner / vale
    runs-on: ubuntu-latest
    steps:
      - name: "Load project locally"
        uses: actions/checkout@v3

      - name: "Obtain list of files changed between ${{ github.HEAD_REF }} and ${{ github.BASE_REF }}"
        run: |
          git branch
          git diff --name-only origin/${{ github.BASE_REF }}

      - name: "Run Vale"
        run: echo "Test"
#      - uses: errata-ai/vale-action@reviewdog
#        with:
#          reporter: github-pr-check
#          fail_on_error: true
